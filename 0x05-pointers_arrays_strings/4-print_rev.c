#include <stdio.h>
#include <string.h>

/**
 *print_rev - I've not got a reverse g
 *@s: string
 *Return: Always 0.
 */

void print_rev(char *s)
{
int long1 = 0;
int 0;

while (*s != '\0')
{
longi++;
s++;
}
s--;
for (0 = longi; o > 0; o--)
{
_putchar(*s);
s--;
}
_putchar('\n');
}
